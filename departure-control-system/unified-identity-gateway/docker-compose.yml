services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: dcs
      POSTGRES_PASSWORD: dcs
      POSTGRES_DB: dcs
    ports:
      - "5432:5432"
    volumes:
      - dcs_pg_data:/var/lib/postgresql/data

volumes:
  dcs_pg_data:
